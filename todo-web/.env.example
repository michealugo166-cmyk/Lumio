# Set this in Netlify's build environment variables to your deployed
# Render backend URL (e.g. https://lumio-backend.onrender.com).
# Left unset, it falls back to http://localhost:4000 for local dev.
VITE_API_URL=
